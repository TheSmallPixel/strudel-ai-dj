{
  "name": "@strudel-ai-dj/provider-spotify",
  "version": "0.0.0",
  "private": true,
  "description": "Spotify provider: OAuth PKCE + read-only Web API (currently-playing, audio-analysis, audio-features).",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint placeholder for @strudel-ai-dj/provider-spotify'",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@strudel-ai-dj/dj-core": "workspace:*"
  }
}
